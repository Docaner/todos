import styled from 'styled-components';

const Title = styled.div`
	width: 100%;
	text-align: center;
	font-weight: 100;
	font-size: 128px;
	color: #a1a1a1;
	margin-top: 100px;
`;

export default Title;

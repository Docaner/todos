import styled from 'styled-components';

const TitleText = styled.div`
	width: 100%;
	position: absolute;
	bottom: 0;
	text-align: center;
	font-weight: 100;
	font-size: 128px;
	color: #a1a1a1;
`;

export default TitleText;
